..\node_modules\.bin\jsdoc -c jsdoc.conf.json --verbose REST_API.js

git push https://hidega:Archangelsk123@github.com/hidega/ permian.git

git commit --author="hidega <hidega@caesar.elte.hu>" -m "dev"

There is a built-in request size limit enforced by Node. Requested headers + URI should not be more than 80 kb.
As it is defined in http_parser.h#L55:
/* Maximium header size allowed */
#define HTTP_MAX_HEADER_SIZE (80*1024)


https://mariadb.com/kb/en/server-system-variables/

git filter-branch -f --env-filter "  GIT_AUTHOR_NAME='hidega'     GIT_AUTHOR_EMAIL='hidega@caesar.elte.hu'     GIT_COMMITTER_NAME='hidega'     GIT_COMMITTER_EMAIL='hidega@caesar.elte.hu'  " HEAD