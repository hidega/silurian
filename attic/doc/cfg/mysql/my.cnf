[mysqld] 
port=3306 
datadir=/opt/app/data/mysqldata
log_syslog=0
general_log=on 
general_log_file=/opt/app/data/mysqldata/mysql.general.log
default-time-zone=+01:00
#bind-address=95.140.42.5

# NOTES
#
# To initialize a fresh data directory 
#   su mysql -c "./bin/mysqld --defaults-file=/opt/app/prg/mysql/my.cnf --console --initialize"
#
# A temporary password is generated for root@localhost: Rwe(uR-gD9Tl
#
# start mysqld
#
# su mysql -c "mysql -u root -p --defaults etc."
#
# ALTER USER 'root'@'localhost' IDENTIFIED BY 'MyNewPass';
#
# root u: root@localhost  p: Pass1234
#
# create database middleware;
#
# SELECT User FROM mysql.user;
#
# CREATE USER 'mwuser'@'localhost' IDENTIFIED BY 'Pass1234';
# GRANT ALL PRIVILEGES ON middleware.* TO 'mwuser'@'localhost' WITH GRANT OPTION;
#
# jdbc:mysql://localhost:3306/middleware?verifyServerCertificate=false&useSSL=true
#
# mwuser pwd: kghBol3786vl1
# root pwd: Hkgh678gjhg8yx
#
#
